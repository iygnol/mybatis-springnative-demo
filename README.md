# mybatis-springnative-demo

# install demo project
mvn clean install

# run or package mybatis-springnative-demo
mvn package -Pnative -DskipTests

#tips
The path is wrong when mybatis scans the xml in the jar package (Windows 10).
